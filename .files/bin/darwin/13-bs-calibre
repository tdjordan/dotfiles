#!/bin/bash

# update brew receipe list
brew update

# tap the cask
brew tap caskroom/cask

# tap cask versions
brew tap caskroom/versions

# install xmind
brew cask install calibre

# cleanup
brew cleanup
